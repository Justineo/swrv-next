# Fix CI error #34

- Reproduce the failure with the current repo validation flow.
- Replace Vite+ config files that rely on native TypeScript loading with JavaScript module configs where needed.
- Update script and tooling references to the renamed config files.
- Re-run the affected validation commands and record the result.
