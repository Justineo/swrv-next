# CI fix for PR #6 and #9

## Goal

Identify the failing GitHub Actions checks on PR #6 and PR #9, determine whether they share a root cause, and apply the smallest correct repository fix.

## Steps

1. Inspect PR metadata, changed files, failing checks, and Actions logs for #6 and #9.
2. Reproduce the relevant failure locally with Vite+ commands where possible.
3. Implement the focused fix in the affected source, configuration, or validation path.
4. Run the project-required validation: `vp check` and `vp test`, plus any narrower command that reproduces the CI failure.
5. Record the root cause, fix, and remaining verification state in the journey log.
