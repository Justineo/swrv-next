# Fix CI error #34

- Reproduced the task-environment failure with `./node_modules/.bin/vp check`.
- The failure happened before formatting or linting because Vite+ tried to load `vite.config.ts` through Node's native TypeScript config path.
- Renamed the root and `packages/swrv` Vite+ config entrypoints to `.mjs` and updated direct references so repo validation no longer depends on TS config loading support.
- Root `vp check` then exposed missing Node globals for `playwright.config.ts`, so the root `tsconfig.json` now includes the Node type package just like the package-local configs.
